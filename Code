const agregarItemAlFinal = (arreglo, item) =>{
    arreglo.push(item);
  
    return console.log('Se retorna el arreglo con el nuevo item: ', arreglo);
  }

  agregarItemAlFinal([20, 1, "z", 3, 1, 5, 12], true);
